<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Dashboard</title>
</head>
<body class="bg-gray-100 flex flex-col min-h-screen">

    <!-- Navbar -->
    <%- include("./partials/navbar.ejs") %>

    <!-- Main Content -->
    <main class="flex-grow flex items-center justify-center p-6">
        <div class="w-full max-w-3xl bg-white shadow-md rounded-lg p-8">
            <h1 class="text-4xl font-bold text-gray-800 mb-6">Welcome to the store!</h1>

            <!-- User Table -->
            <table class="min-w-full bg-white border rounded-lg">
                <thead>
                    <tr class="bg-gray-200">
                        <th class="py-3 px-4 border-b-2 text-left text-gray-600 font-semibold">Product Name</th>
                        <th class="py-3 px-4 border-b-2 text-left text-gray-600 font-semibold">Price</th>
                        <th class="py-3 px-4 border-b-2 text-left text-gray-600 font-semibold">Quantity</th>
                        <th class="py-3 px-4 border-b-2 text-left text-gray-600 font-semibold">View Product</th>
                    </tr>
                </thead>
                <tbody>
                    <% data.forEach(product => { %>
                        <tr class="hover:bg-gray-100 border-b">
                            <td class="py-3 px-4 text-gray-700"><%= product.productName %></td>
                            <td class="py-3 px-4 text-gray-700"><%= product.price %></td>
                            <td class="py-3 px-4 text-gray-700"><%= product.quantity %></td>
                            <td class="py-3 px-4 text-gray-700"><button type="button"><a href="/products/<%= product.id %>">View</a></button></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </main>

</body>
</html>
